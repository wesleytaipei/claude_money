{'type': 'text', 'text': 'Now I have enough context. Let me compile my findings into a structured audit report:\n\n## HC Finance Project Audit Report\n\nBased on thorough review of backend (FastAPI + info_scraper.py) and frontend (vanilla JS) codebases, here are the key issues:\n\n---\n\n### **1. CODE QUALITY & ERROR HANDLING**\n\n#### **Backend (main.py)**\n\n- **Line 101-104**: Price cache TTL is hardcoded (300s). No graceful degradation if cache expires during market hours—fetches will block UI. When fetching multiple TW stocks, MIS API can time out (line 460–527).\n\n- **Line 275–281**: `_is_tw_market_open()` uses hardcoded 9:00–13:30 range, ignoring auctions (8:30–9:00, 13:30–14:30 pre-close). Stale prices will be served during auction/after-hours.\n\n- **Line 412–419**: Regex-based HTML scraping for Yahoo Finance is fragile. Pattern `"regularMarketPrice":([0-9]+\\.?[0-9]*)` fails if Yahoo changes JSON structure; no fallback if regex is empty.\n\n- **Line 459–486**: MIS API response parsing assumes `msgArray` field exists. No validation—if field is missing, loop silently skips data (returns cached miss or None).\n\n- **Line 700–818**: CB suspension fetch has catastrophic chain: JSON API → fallback CSV HEAD check → CSV parse. If all fail, returns empty dict (not cached error). Next request tries again 1 hour later. Meanwhile, suspended CBs show no warning.\n\n- **Line 1748–1822**: TWSE disposition fetch (processing disposition stocks) uses `cp950` decode fallback to `replace` mode. Mangled stock names are silently used. No validation that `end_date < today` actually parsed.\n\n- **Line 1920–1949**: ezmoney ETF Excel parsing assumes specific layout (header at row N). If layout changes, `_parse_roc_date()` returns empty string, silently failing.\n\n- **Line 2132–2156**: Foreign stock price fetching via Yahoo scrape (line 2088–2119) uses fragile regex for price/currency. If currency field is missing, falls back to hardcoded currency—wrong for non-USD assets.\n\n#### **info_scraper.py**\n\n- **Line 16**: Hardcoded Firecrawl API key in source code! Major security breach. Should be environment variable.\n\n- **Line 68–73**: `_safe_float()` silently returns None for any parse error. Downstream code doesn\'t distinguish between "no data" and "bad parse"—both become None.\n\n- **Line 686–816**: Chip data scraper from norway.twsthr.info:\n  - No rate-limiting or retry logic; if site is slow, 10 concurrent requests will hang.\n  - Regex parsing assumes exact HTML structure; any minor change breaks it.\n  - Returns `{"error": "..."}` on failure, but frontend treats this as valid data (line app.js doesn\'t check `_chipCache[sym].error`).\n\n- **Line 819–852**: `scrape_important_info()` uses ThreadPoolExecutor with no timeout. If any sub-fetch hangs, entire pool blocks 10 workers.\n\n---\n\n#### **Frontend (app.js)**\n\n- **Line 179–194**: `evalExpr()` uses `Function()` constructor—vulnerable to injection if user input contains semicolons (e.g., `"1+2;alert(\'xss\')"` will execute). Regex check `^[\\d\\s+\\-*/.()%]+$` blocks letters but not semicolons.\n\n- **Line 338–340**: P&L % calculation divides by cost with no zero-check: `item._pnl_pct = cost > 0 ? (item._pnl / cost * 100) : 0`. If cost is 0 (blank entry), returns 0, hiding the edge case.\n\n- **Line 489–562**: `refreshPrices()` makes 3 parallel API calls for TW/US/CB prices but doesn\'t debounce rapid clicks. User can spam refresh button, creating 100+ concurrent requests in 2 seconds.\n\n- **Line 1145**: Chip data batch endpoint marks symbols as `null` while loading: `_chipCache[s] = null`. Frontend then tries to call `_chipCellHtml(_chipCache[sym])` with null, causing undefined method errors.\n\n- **Line 1396–1425**: Modal auto-fill for CB lookup uses blur event, but doesn\'t cancel prior in-flight requests. If user blurs → re-enters symbol twice rapidly, 1st response might overwrite 2nd.\n\n---\n\n### **2. PERFORMANCE BOTTLENECKS**\n\n- **Line main.py:315–320**: TW stock MIS API fetch with ThreadPoolExecutor uses `max_workers=min(8, len(to_fetch))`. For 50 stocks, will fire 8 parallel requests. If one is slow (timeout 10s line 464), entire batch blocks for 10s—UI frozen.\n\n- **Line main.py:1331–1333**: TW stock table rebuild happens every Sunday at 15:00. Fetches 5 ISIN pages sequentially (no parallelism). With timeout 30s each, takes up to 150s. During rebuild, any stock lookup returns stale data.\n\n- **Line main.py:2122–2157**: `_etf_enrich_prices()` called for every ETF holding enrichment. Fetches TW + foreign prices via MIS + Yahoo scrape. For 00981A (100+ holdings), this is 100 concurrent requests. Market hours (TTL 60s) means every page load triggers fresh fetch.\n\n- **Line app.js:489–507**: TW stock price refresh joins symbols with `encodeURIComponent()`. For 50 symbols, URL becomes `?symbols=2330,00988A,4931,...` (>2KB). Some proxies reject >4KB URLs.\n\n---\n\n### **3. UX/FEATURE GAPS**\n\n- **No error messaging for stale prices**. If MIS API is down, frontend shows 1-hour-old prices without warning.\n\n- **Margin auto-sync (line app.js:354–400)** only works inbound (TW stock `margin_amount` → liabilities). If user manually edits "股票融資" liability, `margin_amount` doesn\'t sync back. Creates inconsistency.\n\n- **CB suspension status not shown until user visits 可轉債 tab** (line app.js:977–992). Should pre-fetch on page load like important-info.\n\n- **No position lock/archive**. User can accidentally delete a 20-year-old position with 1 click (line app.js:1285–1293). No undo.\n\n- **Gist sync is "silent unless error"** (line app.js:1575–1615). If local is newer, user must manually "long-press" button for force pull. Confusing UX.\n\n- **No dark mode toggle** (CSS has `--dark` vars but no button). For private wealth dashboard, night mode essential.\n\n- **ETF tracking only works for 5 hardcoded ETFs** (line main.py:1874–1880). Adding new ETF requires code change + server restart. User can\'t track custom ETFs.\n\n---\n\n### **4. DATA ACCURACY CONCERNS**\n\n- **TW stock change_pct sourced from 4 places with conflicts**:\n  - MIS API item.z/y → change_pct (lines 362–365)\n  - Yahoo scrape regex (lines 383–419)\n  - yfinance previous_close (lines 507–508)\n  \n  No consistency check. User sees different pct on portfolio vs investment grid.\n\n- **CB remaining shares outdated**:\n  - CBAS API returns `circulating_balance` (can lag 3+ days)\n  - Overridden by TDCC CSV (line 978–985)\n  - But TDCC fetch fails silently (line 694–696), falls back to CBAS. User doesn\'t know which source.\n\n- **Margin ratio calculation (line info_scraper.py:402–501)** uses:\n  - TWSE MI_MARGN (per-stock margin lots) + STOCK_DAY_ALL (closing prices) + total debt summary\n  \n  If one API lags, ratio is stale. No warning indicator. User sees "維持率 180%" without knowing data is 6 hours old.\n\n- **TSM ADR used as proxy for 2330 (line app.js:1093–1101)** but TSM trades in USD; 2330 trades in TWD. Exchange rate mismatch ignored in alert logic.\n\n---\n\n### **5. CONCURRENCY & RACE CONDITIONS**\n\n- **Global `_tw_mis_cache` is not thread-safe** (line main.py:102–103). Scheduler runs `_clear_tw_price_cache_for_open()` at 9:01 AM. If user clicks refresh at 9:00:59, race condition: cache is cleared while fetch is in-flight.\n\n- **Gist sync + portfolio edit race**:\n  - User edits portfolio → saves locally (removes `_last_modified`, line 213)\n  - Scheduler pulls from Gist (checks timestamps)\n  - User\'s local edits might be overwritten if Gist is older\n  - No conflict resolution beyond "Gist timestamp wins"\n\n- **Backend `_etf_tracking_cache` is per-fund but shared across requests** (line main.py:1871). If two requests try to warm cache simultaneously, both fetch same data, wasting bandwidth.\n\n---\n\n### **6. HARDCODED VALUES & CONFIG**\n\n- **Line main.py:270**: Market hours hardcoded; doesn\'t account for holidays.\n- **Line main.py:651**: CB suspension pre-warning 10 days (WARN_DAYS) hardcoded—not configurable.\n- **Line main.py:1254**: TW stock table updated only via manual rebuild or weekly cron. No auto-refresh trigger when new stock is added to market.\n- **Line app.js:14–18**: Exchange rate (USD/JPY) hardcoded default (31.77/0.2). If hardcoded value is stale and API fails, user sees ancient rates.\n\n---\n\n### **7. MISSING FEATURES FOR INVESTOR USE**\n\n- **No watchlist or price alerts**. User must manually refresh to catch limit-down events.\n- **No tax lot tracking**. All cost basis is "average cost"—can\'t compute FIFO/LIFO for tax reporting.\n- **No dividend/interest income tracking**. Portfolio shows only market value, not total return.\n- **No dividend ex-date warnings**. User might miss ex-div cutoff for margin-held stocks.\n- **No margin call risk indicator**. Shows leverage but not maintenance margin requirement vs. collateral.\n- **CB conversion auto-exercise not tracked**. If CB is forced converted (dropped below floor), portfolio becomes unaware.\n\n---\n\n### **8. INFRASTRUCTURE ISSUES**\n\n- **HTTPS disable (line main.py:17, 464, 675)**: `verify=False` everywhere. Vulnerable to MITM attacks on public WiFi.\n- **No API rate-limiting**. User can DOS own server by rapidly clicking refresh.\n- **No request logging**. If API fails at 3 AM via scheduler, no audit trail.\n- **Scheduler has no error recovery** (line main.py:1628–1631). If `compute_and_save_snapshot()` crashes, next run at 15:00 tomorrow—8+ hours gap.\n\n---\n\n### **Critical Priorities to Fix**\n\n1. **Remove hardcoded API key** from info_scraper.py line 16\n2. **Add null checks** before all DOM manipulations (app.js line 1145)\n3. **Debounce refresh buttons** to prevent request storms\n4. **Cache CB suspensions locally** so status is shown without network delay\n5. **Add "data source" badges** to prices (MIS/Yahoo/yf/cached) so user knows freshness\n6. **Implement position soft-delete** (archive) not hard delete\n7. **Validate margin sync bidirectionally** (cost ↔ margin_amount)\n8. **Add scheduler error hooks** to notify on midnight auto-snapshot failure\n9. **Cache CB metadata separately** from suspension status (different TTLs)\n10. **Use environment-based config** for all hardcoded values (market hours, suspension warning, cache TTLs)'}